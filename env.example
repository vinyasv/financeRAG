# Finance RAG Configuration
# Copy this file to .env and fill in your values:
#   cp env.example .env

# =============================================================================
# API Key (required)
# =============================================================================

# OpenRouter (recommended - one key for LLM + embeddings)
# Get your key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Or use direct provider APIs (optional):
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# =============================================================================
# LLM Model (optional)
# =============================================================================

# Default LLM model for query planning and synthesis
# Options: gpt-4o-mini, claude-sonnet, gemini-flash, llama-70b, etc.
# LLM_MODEL=gpt-4o-mini

# =============================================================================
# Vision Model for Table Extraction (optional)
# =============================================================================

# Model used to extract tables from PDF images
# Must be a vision-capable model. Recommended options:
#
#   google/gemini-2.0-flash-001     (RECOMMENDED - fast, accurate, cheap)
#   openai/gpt-4o                   (best quality, slower, more expensive)
#   openai/gpt-4o-mini              (good balance)
#   anthropic/claude-3.5-sonnet     (excellent quality)
#
# VISION_MODEL=google/gemini-2.0-flash-001

# Disable vision-based table extraction (use rule-based instead):
# USE_VISION_TABLES=false

# =============================================================================
# Embedding Model (optional)
# =============================================================================

# Default embedding model (defaults to qwen/qwen3-embedding-8b via OpenRouter)
# 
# OpenRouter models (requires OPENROUTER_API_KEY):
#   qwen/qwen3-embedding-8b         (default, good quality, fast)
#   openai/text-embedding-3-small   (OpenAI, good balance)
#   openai/text-embedding-3-large   (OpenAI, best quality)
#   cohere/embed-english-v3.0       (Cohere)
#   google/text-embedding-004       (Google)
#
# Local models (free, no API key needed):
#   all-MiniLM-L6-v2               (fast, good for testing)
#   all-mpnet-base-v2              (better quality)
#
# EMBEDDING_MODEL=qwen/qwen3-embedding-8b

# Force local embeddings even if OPENROUTER_API_KEY is set:
# EMBEDDING_PROVIDER=local

